# Domain Structure

- `Accounts/` keeps authentication, roles, and school account concerns.
- `NSTP/` is reserved for upload, parse, validate, approve, and serial-number workflows in Phases 1-7.
- `Shared/` is reserved for DTOs, enums, and reusable contracts.